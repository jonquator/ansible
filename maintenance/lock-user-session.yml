---
- name: Bloquear la sesión de usuarios en equipos Windows
  hosts: all
  gather_facts: no
  tasks:
    - name: Bloquear la sesión del usuario actual en Windows
      win_command: powershell.exe -Command "rundll32.exe user32.dll, LockWorkStation"
      ignore_errors: yes
